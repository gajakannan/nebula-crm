# JSON Schema Forms Guide

Generic guidance for dynamic form rendering with JSON Schema.

## When to Use

- Complex, dynamic forms
- Need to share schema between frontend and backend

## Recommended Approach

- Define schemas in a shared location
- Validate with AJV (or similar)
- Render with a JSON Schema form library

---

See `planning-mds/examples/` for project-specific schema examples.
