# WSL-mounted Windows filesystems are unreliable for pnpm's rename-heavy
# virtual store operations. Keep the virtual store on the Linux filesystem.
virtual-store-dir=${HOME}/.pnpm-virtual-store/nebula-crm-experience
